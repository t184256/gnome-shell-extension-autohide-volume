nix build -L './tests#test'
gnome-extensions pack . --force --extra-source=LICENSE --extra-source=README.md
diff <(unzip -Z1 autohide-volume@unboiled.info.shell-extension.zip | sort) \
     <(printf '%s\n' LICENSE README.md extension.js metadata.json)

# in case of screenshots changing
nix build -L './tests#screenshots'
cat result/initial_corner.png > tests/reference/initial.png
cat result/unmuted_corner.png > tests/reference/unmuted.png
cat result/muted_corner.png > tests/reference/muted.png
git diff --exit-code --stat HEAD tests/reference
